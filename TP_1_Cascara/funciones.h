#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED

/**
 * \brief Solicita dos numeros al usuario y devuelve el resultado.
 * \param a solicita el operando 1.
 * \param b solicita el operando 2.
 * \return El resultado de la suma de a y b.
 *
 */
int sumar(int a, int b);

/**
 * \brief Solicita dos numeros, verifica y devuelve true resultado o false advertencia.
 * \param a solicita el operando 1.
 * \param b solicita el operando 2.
 * \return El resultado de la suma de a y b.
 *
 */
void calcuSuma(int a, int b);

/**
 * \brief Solicita dos numeros y devuelve el resultado.
 * \param a solicita el operando 1.
 * \param b solicita el operando 2.
 * \return El resultado de la resta de a y b.
 *
 */
int restar(int a, int b);

/**
 * \brief Solicita dos numeros, verifica y devuelve true resultado o false advertencia.
 * \param a solicita el operando 1.
 * \param b solicita el operando 2.
 * \return El resultado de la resta de a y b.
 *
 */
void calcuRestar(int a, int b);

/**
 * \brief Solicita dos numeros y devuelve el resultado.
 * \param a solicita el operando 1.
 * \param b solicita el operando 2.
 * \return El resultado de la division de a y b.
 *
 */
float dividir(float a, float b);

/**
 * \brief Solicita dos numeros, verifica y devuelve true resultado o false advertencia.
 * \param a solicita el operando 1.
 * \param b solicita el operando 2.
 * \return El resultado de la division de a y b.
 *
 */
void calcuDiv(float a, float b);

/**
 * \brief Solicita dos numeros y devuelve el resultado.
 * \param a solicita el operando 1.
 * \param b solicita el operando 2.
 * \return El resultado de la multiplicación de a y b.
 *
 */

int multiplicar(int a, int b);

/**
 * \brief Solicita dos numeros, verifica y devuelve true resultado o false advertencia.
 * \param a solicita el operando 1.
 * \param b solicita el operando 2.
 * \return El resultado de la multiplicación de a y b.
 *
 */
void calcuMulti(int a, int b);

/**
 * \brief Solicita dos numeros y verifica si son true o false.
 * \param a solicita el operando 1.
 * \param b solicita el operando 2.
 * \return true o false.
 *
 */
int verificacion(int a, int b);

/**
 * \brief Solicita un numero y devuelve el resultado.
 * \param a solicita el operando 1.
 * \return El factorial de a.
 *
 */
float factorial(float a);

#endif // FUNCIONES_H_INCLUDED

